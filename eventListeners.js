// Event listener for keyboard inputs (e.g., arrow keys for navigation)
document.addEventListener('keydown', (event) => {
    //to handle keyboard inputs
});

// Event listener for mouse clicks (e.g., to zoom in on an artwork or view additional information)
document.addEventListener('click', (event) => {
    //to handle mouse clicks
});
